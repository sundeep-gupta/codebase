Schema for Tiffin box Portal;
===============================

TABLE USERS(ID, USERID, PASSWORD, NAME, EMAIL ID, PHONE, A.NUMBER, A.SOCIETY, A.AREA, A.CITY, A.COUNTRY, A.PIN);

TABLE CUISINE(MENUID, TYPE, SUBCOST)

TABLE SUBSCRIPTION(SUBID, ID, USERID, MENUID, TYPE, ACBAL, ACSTATUS, SDATE)

TABLE DELIVERY(ID, MENUID, TYPE, DATE, COST)

