#include <iostream>

//write a program to print in words the single or double digit numbers entered.(do not use switch case)

char* spellNumber(int num) {
	if (num > 99 || num < 0) {
		cout <<"spellNumber can handle 0-99 only" << endl;
	}
	if ((number/10) == 1) {

	}

}
